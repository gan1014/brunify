{
    "name": "brunify-monorepo",
    "version": "1.0.0",
    "type": "module",
    "private": true,
    "workspaces": [
        "frontend",
        "backend"
    ],
    "scripts": {
        "build": "npm run build --workspace=frontend && mkdir -p public && cp -r frontend/dist/. public/"
    }
}